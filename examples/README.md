# Rendered examples

These PDFs are generated from the canonical projects in `../projects/` and are committed only as convenient visual references.

- [Field-study report](rendered/field-study-report.pdf)
- [Workshop handbook](rendered/workshop-handbook.pdf)

To regenerate and fully verify them:

```powershell
.\Document-System.cmd build shade-study
.\Document-System.cmd build clear-decisions
```

The authoritative editable templates are the manifests and canonical sources, not these rendered PDFs.
